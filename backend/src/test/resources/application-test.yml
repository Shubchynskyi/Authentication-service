spring:
  mail:
    host: localhost
    port: 587
    username: test@test.com
    password: test
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false

access:
  mode:
    default: WHITELIST

admin:
  enabled: false
  email: admin@test.com
  username: admin

logging:
  level:
    root: WARN
    '[com.authenticationservice]': INFO

testcontainers:
  docker:
    api-version: "1.24"
  ryuk:
    enabled: false
  checks:
    disable: true